version: 2

models:
  - name: int_localbikes__sales_orders
    description: '{{ doc("int_localbikes__sales_orders") }}'
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null
